Test addon.

The addon will list files in /var/log and create artifacts for each file.

Set current directory to project root.

Environment (required):
HUB = "http://localhost:8008"
SECRET = "hack/cmd/addon/hub.json"
